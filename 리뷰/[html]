1. select 태그
- select에는 name 속성, option에 value 속성을 사용한다
- option 요소의 목록값은 텍스트에서 정할 수 있으며, 서버에 전달되는 value 값과
역할이 다르다

정리 : option태그 텍스트 : 항목
                  value  : 서버에 전달되는 파라미터의 값  

2. <img> 태그는 인라인 요소다